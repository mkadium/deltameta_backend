# Trino PostgreSQL connector — connects to your external Postgres
connector.name=postgresql
connection-url=jdbc:postgresql://${PRIMARY_DB_HOST}:${PRIMARY_DB_PORT}/${PRIMARY_DB_NAME}
connection-user=${PRIMARY_DB_USER}
connection-password=${PRIMARY_DB_PASSWORD}
